const weighted = @import("font_atlas_weighted.zig");

pub const width = weighted.width;
pub const height = weighted.height;
pub const bytes = weighted.bytes;
pub const channels = weighted.channels;
pub const glyph_capacity = weighted.glyph_capacity;
pub const format = weighted.format;
pub const Atlas = weighted.Atlas;
